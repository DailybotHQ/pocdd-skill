# POC working state — never commit. POCDD files are per-developer, per-feature
# context, not source.
.pocs/

# Local configure profile (machine-specific). Re-run ./configure to regenerate.
.pocdd/

# Python / tooling noise
__pycache__/
*.py[cod]
.venv/
venv/
node_modules/

# OS / editor
.DS_Store
*.swp
tmp/
